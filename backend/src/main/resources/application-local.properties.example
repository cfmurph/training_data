# Local development overrides — never commit secrets here
# Usage: mvn spring-boot:run -Dspring-boot.run.profiles=local

# Cookie does not need Secure flag over plain HTTP in local dev
server.servlet.session.cookie.secure=false

# Verbose logging in dev is fine
logging.level.com.traintrack=DEBUG
