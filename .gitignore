node_modules/
dist/
.env
*.log
.DS_Store
*.tsbuildinfo

# Maven / Java
backend/target/
*.class
*.jar
*.war

# H2 file-based database (local runtime data, not source)
backend/data/

# Local config (contains secrets — never commit)
backend/src/main/resources/application-local.properties
